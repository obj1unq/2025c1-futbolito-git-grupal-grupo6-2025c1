import objetos.*
import pgmProgram.*

describe "requerimientos futbolito" {
  test "Levantarla" {
    assert.notEquals(lionel.position(), pelota.position())
    lionel.position(game.at(3, 5))
    pelota.position(game.at(3, 5))
    assert.equals(lionel.position(), pelota.position())
  }
}